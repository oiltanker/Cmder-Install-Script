# Cmder-Install-Script
Batch script that installs all variables and text menu system entires for Cmder.

## Usage

1. Add both files into Cmder executable directory.
2. Run `install.bat` with administrator rights.
